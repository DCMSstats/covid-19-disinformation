# A test to collect a large amount of subbreditsimport prawimport pandas as pdimport datetime as dtimport sysimport numpy as npimport disinfo.functions as di#set search termsconsp_theory = "5g "terms_list = ["coronavirus", "covid", "corona", "pandemic", "sars-cov-2",                "epidemic", "outbreak", "koronavirus"]search_terms = [consp_theory + c for c in terms_list]print("searching for" + search_terms)def get_subreddits():    df = di.get_subreddir_data(r, ["all"],  limit= 1000)    un = df["subreddit"].unique()    sys.stdout=open("test2.txt","w")    for i in un:        print(i)    sys.stdout.close()    subreddits = np.loadtxt("test2.txt", dtype = 'str')        return(subreddits)hit = get_subreddits() df = di.get_subreddir_data(r, ["all"],  limit= 1000) while i < len(terms_2):    terms_2.insert(i, term_1)    i += 1    print(terms_2)